<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/container_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorPrimary">

    <android.support.design.widget.AppBarLayout
        android:id="@+id/appBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:elevation="0dp"
        android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar">

        <android.support.design.widget.TabLayout
            android:id="@+id/two_way_reservation_fragment_tabs"
            android:layout_width="match_parent"
            android:layout_gravity="left"
            android:paddingRight="@dimen/margin_2x"
            android:paddingLeft="@dimen/margin_2x"
            android:layout_height="?android:attr/actionBarSize"
            app:tabIndicatorColor="@color/tab_indicator_color"
            android:background="@color/colorPrimary"
            app:tabTextAppearance="@style/TabTextAppearance"
            app:tabGravity="fill"
            app:tabMode="fixed"/>
    </android.support.design.widget.AppBarLayout>

    <com.amr.codes.erkeny.views.customeView.CustomViewPager
        android:id="@+id/two_way_reservation_fragment_viewpager"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/appBar" />

</RelativeLayout>
